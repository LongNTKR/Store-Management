# Store Management System - Environment Variables
# Copy this file to .env and configure for your environment

# Company Configuration
COMPANY_NAME=Cửa Hàng Gia Đình

# Optional: Security & Encryption
# Required for AI features (API key encryption in database)
# Must be at least 32 characters long
# SECRET_KEY=your-secret-key-here-min-32-chars

# Optional: Google Cloud Vision API
# For OCR-based price list import and image-based product search
# Path should be relative to backend/ directory in container
# GOOGLE_CREDENTIALS_PATH=/app/credentials.json

# Note: When using Docker, mount your credentials file:
# volumes:
#   - ./path/to/credentials.json:/app/credentials.json:ro
